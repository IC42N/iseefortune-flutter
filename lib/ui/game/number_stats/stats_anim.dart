import 'package:flutter/animation.dart';

const kStatsAnimDuration = Duration(milliseconds: 220);
const kStatsAnimCurve = Curves.easeOutCubic;

// Unified bar intensities (match web feel)
const kBarIntensity = 0.65; // normal
const kBarZeroIntensity = 0.15; // muted but still visible
